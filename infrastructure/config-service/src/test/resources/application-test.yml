server:
    port: 0

spring:
    application:
        name: config-service

    cloud:
        config:
            server:
                native:
                    search-locations:
                        - classpath:/config-repo

management:
    tracing:
        enabled: false
