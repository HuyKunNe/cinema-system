spring:
    cloud:
        config:
            enabled: false

    datasource:
        url: ${INVENTORY_TEST_DB_URL:jdbc:mysql://localhost:3306/cinema_inventory_test_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
        username: ${INVENTORY_TEST_DB_USERNAME:root}
        password: ${INVENTORY_TEST_DB_PASSWORD:huykun17}
        driver-class-name: com.mysql.cj.jdbc.Driver

    flyway:
        enabled: true
        clean-disabled: true

    jpa:
        hibernate:
            ddl-auto: validate
        open-in-view: false
        properties:
            hibernate:
                format_sql: true

eureka:
    client:
        enabled: false
