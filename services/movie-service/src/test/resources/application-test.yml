spring:
    application:
        name: movie-service

    cloud:
        config:
            enabled: false

        discovery:
            enabled: false

    datasource:
        url: jdbc:h2:mem:movie_test_db
        driver-class-name: org.h2.Driver
        username: sa
        password: huykun17

    jpa:
        hibernate:
            ddl-auto: create-drop

    flyway:
        enabled: false

eureka:
    client:
        enabled: false
        register-with-eureka: false
        fetch-registry: false

management:
    tracing:
        enabled: false
